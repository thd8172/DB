SELECT 하나 이상의 열로 구성되는 목록  (중요)
DISTICT 중복되는 열 생략
* 모든 열 선택 (중요)
SELECT * 
FROM emp; 모든열 다 나옴
alias 선택된 열에 다른 머리글 부여  (중요)
FRom Table 해당 열을 포함하는 테이블 지정ㅇ

산순연산자 + -  / * 
이건 아니깐 그냥 넘어가자

NULL 값 정의
알수 없는 값 , 사용할수 없는값, 할당 할수 없는 값, 적용할 수 없는값
널은 0또는 공백과는 다름
열 이름의 별칭은 AS 키워드 사용
공백 또는 특수문자 있거나 대소문자 구분할 경우 큰 따움표(" ")를 사용

열 또는 문자열을 다른 열에 연결
두개의 세로선 ( || )으로 표시
문자식인 결과 열을 생선

리터널은 SELECT 목록에 포함된 문자 숫자 또는 날짜 
날짜 및 문자 리터럴 값은 작은 따옴표(' ')로 표시

중복행 (row) 제거 ( 계속)
SELCT 다음에 DISTICT 키워드를 상용하여 중복 행을 제거
SELECT DISTICT first_name

ALL  모든 값을 출력\

WHERE 절 
사용하여 반환되는 행을 제한
FROM  절 다음에 사용
 예 : 
SELECT ***
FROM ***
WHERE ***
 요렇게 순서대로 사용

문자열 또는 날짜 값응 작은 따옴표(' ')로 묶음
문자 값은 대소문자를 구분하여 날짜 값은 형식을 구분
WHERE *** = 10;
WHERE *** = '대 소문자 구분하깅'
WHERE***= '83/01/12'

연산자  = > >= < <= <> (!=)같지 않음
빼고 자바나 자바스크립트에서 배운거랑 같음

IN연산자
IN 연산자를 사용하여 목록을 조회한다
목록에 문자열이나 날짜형이면 무조건 (' ') 를 사용한다 
여러종목 조회할떄 사용

LIKE 연산자 
 %에는 문자가 오지 않거나 여러 개 올 수 있음
_에는 문자가 하나만 올 수 있음
WHERE *** LIKE '%A'

% 앞에 넣으면 %A 뒷글자가 A일뗴
			A%앞글자가 A 일떄
			%A% 문자열이 있기만 해도됨
_은 문자열을 검색할수 있다

is NULL 연산자를 사용하여 널 값을 조회
값이 없음을 확인 하는데 사용

I AND  연산자
AND 는 조건이 무조건 TRUE 일떄 경우 TRUE
WHERE sal < 3000;
AND diptno = 10;
10번 부서에서 3000불 안되는사람 이라는 뜻


ASC 오름차순 기본값
DESC : 내림차순
ㄴ
